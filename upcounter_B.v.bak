module upcounter_B(input
  
  // miter circuit 
  // multiple xor and then a single or gate at the output
  // upcounter
  // 7-segment
  
  //  May need to add delay into the circuits
  // in the miter circuits there will be spikes, but they will not have a time duration
  // State machines
  // State transition diagrams
  
  
  // 2 State Machine
  // Mealy Machine
  //
  
  // 3 coin sensor
  
  // 3 coin sensor
  // it will tie into the next machine
  // serial input for the next statemachine
  // the coin machine wil have different length
  // sequences of length 4 and sequences of length 5
  
  // Do not need to use state machine, we can have an if-conditional check in the shift register, then adjust the output
  // Setup a task to automatically create a test that we care about 
  
  // 
